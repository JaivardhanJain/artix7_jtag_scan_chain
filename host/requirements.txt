# Host-side JTAG driver dependencies.
#
# ftd2xx requires the FTDI D2XX driver to be installed at the OS level.
# On Windows the VCP driver usually has to be disabled for the device --
# see docs/TROUBLESHOOTING.md.
ftd2xx>=1.3.3
bitstring>=4.0.0
